/**
 * Layout for homeowner tools section.
 *
 * Keeps tools isolated under (public)/tools while inheriting
 * global app layout.
 */

export default function ToolsLayout({ children }: { children: React.ReactNode }) {
  return children
}

